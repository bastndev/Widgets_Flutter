import 'package:flutter/material.dart';

class ListviewPractice extends StatelessWidget {
  const ListviewPractice({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ListviewPractice'),
      ),
    );
  }
}